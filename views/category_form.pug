extends layout

block content
  h1= title
  
  form(method="post") 
    label(for="category") Category: 
    input#category(type="text", name="category", required, value=(category === undefined ? '' : category.name))
    button.blue-btn(type="submit") #{category === undefined ? "Create" : "Update" }